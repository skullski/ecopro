import TemplateSettings from './TemplateSettings';

// Silver Editor = the existing TemplateSettings editor.
// We keep the implementation in TemplateSettings.tsx for now and expose this alias
// so the app can route to a clearer name.
export default TemplateSettings;
