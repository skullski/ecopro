// Legacy vendor dashboard removed — placeholder only.

export default function VendorDashboardOldPlaceholder() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h2 className="text-2xl font-bold">Legacy Dashboard Removed</h2>
        <p className="text-muted-foreground mt-2">The legacy dashboard was removed in favor of the new vendor dashboard. Use the sidebar or <code>/vendor/:id/dashboard</code> route.</p>
      </div>
    </div>
  );
}
