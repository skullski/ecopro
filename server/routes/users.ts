import { Router } from "express";

export const usersRouter = Router();

// ...existing code removed (in-memory db). This router is intentionally left empty.
// If you later add user profile APIs, mount them in server/index.ts explicitly.
