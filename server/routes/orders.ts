import { Router } from "express";

export const ordersRouter = Router();

// ...existing code removed (in-memory db/orders). Use DB-backed endpoints in products.ts or add new endpoints here and mount explicitly.
